package com.example.model

enum class AppWorkspaceMode {
    PUBLIC_MARKET,   // Commercial market (shops, market tenants, PMC tax, general dues)
    PRIVATE_PERSONAL // Private residential flats/rooms (owned by this admin/user)
}
